# pip fallback for the HAFS QPF/ETS framework.
# Prefer the conda env (environment.yml) — cfgrib/eccodes/cartopy need the
# ecCodes C library + GEOS/PROJ, which conda-forge provides cleanly.
numpy
scipy
pyyaml
boto3
botocore
cfgrib
eccodes
xarray
zarr
s3fs
matplotlib
cartopy
seaborn
pillow
scikit-learn
pytest # for running synthetic field tests
# shap  # optional: SHAP importance plots; falls back to permutation importance
